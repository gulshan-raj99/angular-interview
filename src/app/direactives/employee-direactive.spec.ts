import { EmployeeDireactive } from './employee-direactive';

describe('EmployeeDireactive', () => {
  it('should create an instance', () => {
    const directive = new EmployeeDireactive();
    expect(directive).toBeTruthy();
  });
});
