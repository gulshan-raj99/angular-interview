import { CustommPipePipe } from './customm-pipe-pipe';

describe('CustommPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustommPipePipe();
    expect(pipe).toBeTruthy();
  });
});
